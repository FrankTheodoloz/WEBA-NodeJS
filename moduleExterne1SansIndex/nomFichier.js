console.log("\nChargement moduleExterne1SansIndex (spécifier nom fichier)");
//appel d'un module interne (sans './')
var http = require("http");

//affiche les code de status http
console.log(http.STATUS_CODES);
